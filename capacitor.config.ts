import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ioi.englishspeaking",
  appName: "I O I English Speaking",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
